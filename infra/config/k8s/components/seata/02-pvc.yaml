apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seata-pvc
  namespace: nexora-infra
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi
