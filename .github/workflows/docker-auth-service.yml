name: Docker auth-service

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Login to Aliyun Registry
        uses: docker/login-action@v3
        with:
          registry: registry.cn-hangzhou.aliyuncs.com
          username: ${{ secrets.ALIYUN_REGISTRY_USERNAME }}
          password: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}
      
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: tiz-backend/auth-service
          push: true
          tags: |
            registry.cn-hangzhou.aliyuncs.com/nxo/auth-service:latest
            registry.cn-hangzhou.aliyuncs.com/nxo/auth-service:sha-${{ github.sha }}
          build-args: |
            GITHUB_TOKEN=${{ github.token }}
            ALIYUN_MAVEN_USERNAME=${{ secrets.ALIYUN_MAVEN_USERNAME }}
            ALIYUN_MAVEN_PASSWORD=${{ secrets.ALIYUN_MAVEN_PASSWORD }}
            PORT=8101
